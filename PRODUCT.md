# Product

## Register

product

## Users

- 希望自托管并完全掌控个人财务数据的个人与家庭用户。
- 在桌面端进行账户整理、交易复核、报表分析和批量维护的高频用户。
- 在移动端快速记账、转账、调整余额和查看预算的用户。
- 需要通过键盘、屏幕阅读器、高对比模式或减少动态效果完成同等任务的用户。
- 通过受认证的 Agent Skill 或 MCP 客户端自动化财务工作流的高级用户。

## Product Purpose

提供一个可信、隐私优先且可完全自主管理的个人记账系统。用户应能从桌面端、移动端和受认证的 AI 工具安全地创建、查询、修改与删除账户、交易、分类、标签及相关配置，同时保持金额精度、转账关联、余额历史和审计语义正确。

## Brand Personality

像素化、精确、可信、高效。界面可以带有数字账本和经典掌机的触感，但财务数据必须始终清晰、克制、可核验；趣味来自一致的像素语言和即时反馈，而不是牺牲可读性的怀旧装饰。

## Anti-references

- 不做套壳式通用后台：避免双重侧栏、松散层级和同权重按钮挤压核心内容。
- 不做霓虹街机：避免高饱和荧光色、闪烁动画、过小像素字体和影响金额辨识的装饰。
- 不做柔软玻璃拟态 SaaS：避免大面积模糊、渐变文字、漂浮胶囊和无边界浅灰卡片。
- 不依赖颜色表达收入、支出、错误或状态；文字、图标和结构必须同时给出语义。
- 不把复杂财务表单全部塞进超长模态框，也不隐藏关键编辑入口。
- 不让 AI 绕过认证、金额精度、关联交易、数据校验或审计边界。

## Design Principles

1. **数据准确先于装饰。** 金额使用最小货币单位或高精度十进制；余额调整通过可追溯交易完成，不能静默改写历史。
2. **编辑能力显式可见。** 账户、交易、分类和标签均提供清晰的查看、编辑、删除入口；高频编辑不依赖猜测行点击或隐藏菜单。
3. **高频任务一步到位。** 快速记账、转账、余额调整和标签修改在首屏给出主操作，复杂选项渐进披露。
4. **像素风是一套系统语言。** 使用硬边框、阶梯式分隔、有限色板和等宽数字塑造识别度，正文仍保持舒适字号、行高和扫描效率。
5. **跨端共享语义。** 桌面端强调密度、批量操作和分析，移动端强调拇指可达和快速录入；两端使用一致的术语、状态和数据规则。
6. **AI 完整可用但必须受控。** 认证后的 MCP 与 Agent Skill 可执行完整财务 CRUD；所有写操作复用服务层校验，危险操作有明确确认语义并可审计。
7. **空状态负责教学。** 首页、交易、账户和分析页的空状态提供下一步动作和最短上手路径，而不是只显示零值。

## Accessibility & Inclusion

目标为 WCAG 2.1 AA。正文对比度至少 4.5:1，组件边界与焦点指示至少 3:1；全部核心功能支持键盘，图标按钮提供可访问名称，表单使用可见标签和可理解错误提示，动态结果通过状态区域播报。触控目标至少 44×44 CSS 像素，页面支持 200% 缩放，收入与支出不只依赖颜色区分，并尊重 `prefers-reduced-motion`、系统主题和辅助技术。
